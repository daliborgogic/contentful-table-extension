{
  "name": "cf-contentful-ui-country-select",
  "version": "1.0.0",
  "description": "Contentful UI extension to easily select countries",
  "main": "index.js",
  "scripts": {
    "create": ". .env && contentful-extension create --space-id ${SPACE_ID}",
    "update": ". .env && contentful-extension update --space-id ${SPACE_ID} --force"
  },
  "devDependencies": {
    "contentful-extension-cli": "^2.0.0"
  }
}
